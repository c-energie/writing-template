# CLAUDE.md

`AGENTS.md` is the contract for every agent working here, and it is deliberately
tool-neutral. Claude Code does not read that filename on its own, so this file exists
only to pull it in — keep the guidance itself in `AGENTS.md`.

@AGENTS.md
