% Generated table library for the Example section.
%
% Written by the analysis template's save_table(): one file per section,
% accumulating every table that section uses, each bracketed by
% catchfilebetweentags markers so it can be pulled into the prose with
%
%     \ExecuteMetaData[Sections/Example/tables.tex]{<tag>}
%
% Re-saving a table replaces its block in place rather than appending a second
% copy, so this file is safe to regenerate. Edit the notebook, not this file —
% anything you change here is overwritten on the next save.

%<*example_summary>
\begin{table}[H]
    \centering
    \begin{tabular}{lrr}
    \toprule
    group & n & mean \\
    \midrule
    a & 24 & 10.42 \\
    b & 18 & 12.87 \\
    \bottomrule
    \end{tabular}
    \caption{Placeholder summary, replaced the first time the example notebook runs.}
    \label{Tab: example_summary}
\end{table}
%</example_summary>
